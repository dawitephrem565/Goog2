<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.filenber.goog">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login_activity"
            android:theme="@style/Actionbar" />
        <activity android:name=".Frameholder" />
        <activity
            android:name=".bottomnavigation"
            android:label="@string/app_name"
            android:theme="@style/Actionbar" />
        <activity android:name=".CreateChannel"
            android:theme="@style/Actionbar"
            />
        <activity android:name=".Channel_Fragmet_Page"
            android:theme="@style/Actionbar"
            />
        <activity android:name=".PostIdea"
            android:theme="@style/Actionbar"
            ></activity>
    </application>

</manifest>